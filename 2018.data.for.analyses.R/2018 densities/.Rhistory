anova(mod2i,type="II")
anova(mod2i)
summary(mod2i)
summary(mod2)#for trial 6, no sig. interaction term
plot(mod2a)
#for 2018, trial 6, removing interaction term and reanalyzing
mod2i<-lm(Density~Treatment+Week, data=df)
hist(resid(mod2i))
qqnorm(resid(mod2i))
qqline(resid(mod2i))
anova(mod2i)
plot(mod2i)
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
mod2<-lm(Density~Treatment*Week, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
summary(mod2)#for trial 6, no sig. interaction term
mod2ii<-lm(Density~Treatment*Week+(1|Week:Day), data=df)
hist(resid(mod2ii))
mod2<-lm(Density~Treatment*Week, data=df)
anova(mod2)
anova(mod2i)
#2018 trial 6 plots
bargraph.CI(x.factor = Trial, response = Density, group= deployment.day, legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Trial", ylab="Number of fish seen per reef (mean +/- se)", data = df)
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Trial", ylab="Number of fish seen per reef (mean +/- se)", data = df)
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Trial", ylab="Number of fish seen per reef (mean +/- se)", data = df)
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)", data = df)
df$Treatment<-ordered(df$Treatment, c("Low","Medium","High","Control"))
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)", data = df)
plot(mod2i)
View(df)
dfo<-df[-c(759,744,775),]
mod2<-lm(Density~Treatment*Week, data=dfo)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
plot(mod2)
View(dfo)
dfo<-df[-c(759,744,775),]
dfo<-df[-c(759,772,775),]
dfo<-df[-c(759,772,775), ]
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)", data = df)
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by deployment day", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
lineplot.CI(Week,Density,group=Treatment,legend = TRUE,main="2018,trial 6, density by treatment and week", xlab="Week", ylab="Number of fish per reef (mean +/- se)", data=df)
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
#2018 trial 6 plots
bargraph.CI(x.factor = Week, response = Density, group=Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
lineplot.CI(Week,Density,group=Treatment,legend = TRUE,main="2018,trial 6, density by treatment and week", xlab="Week", ylab="Number of fish per reef (mean +/- se)", data=df)
#just looking at week 1 for trial 6
df<-den.t6[(den.t6$Week==2),]
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
summary(mod2)
#model building
mod1<-lm(Density~Treatment, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
summary(mod1)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Treatment, response = Density, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
#just looking at week 1 for trial 6
df<-den.t6[(den.t6$Week==1),]
#model building
mod1<-lm(Density~Treatment, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
summary(mod2)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Treatment, response = Density, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
df$Treatment<-ordered(df$Treatment, c("Low","Medium","High","Control"))
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=8, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=10, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=9, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=10, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=11, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=10.5, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and week", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=11, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and day", xlab="Week", ylab="Number of fish seen per reef (mean +/- se)",yleg=11, data = df)
#2018 trial 6, just week 1
bargraph.CI(x.factor = Day, response = Density,group = Treatment, legend=TRUE, main="2018,trial 6, densities by treatment and day, week 1 data only", xlab="Day", ylab="Number of fish seen per reef (mean +/- se)",yleg=11, data = df)
bargraph.CI(x.factor = Day, response = Density, group= Treatment, legend=TRUE, main="2018, trials 4 and 5 density by treatment and week", xlab="Week", ylab="Number of fish per reef (mean +/- se)", data = df)
df<-den.t6
bargraph.CI(x.factor = Day, response = Density, group= Treatment, legend=TRUE, main="2018, trials 4 and 5 density by treatment and week", xlab="Week", ylab="Number of fish per reef (mean +/- se)", data = df)
df$Treatment<-ordered(df$Treatment, c("Low","Medium","High","Control"))
bargraph.CI(x.factor = Day, response = Density, group= Treatment, legend=TRUE, main="2018, trials 4 and 5 density by treatment and day", xlab="Day", ylab="Number of fish per reef (mean +/- se)", data = df)
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
#just looking at week 1 for trial 6
df<-den.t6[(den.t6$Week==1),]
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
library(sciplot)
library(lme4)
library(car)
library(ggplot2)
library(lmerTest)
library(dplyr)
gob.den<-read.csv("Data/density.1.15.19.csv")
#setting up df for different years (will also include all of the figures for 2018 trials separately)####
den.t1.2.3<-gob.den[gob.den$Trial<4, ]
den.t4.5<-gob.den[(gob.den$Trial>3)&(gob.den$Trial<6), ]
den.t6<-gob.den[gob.den$Trial>5,]
df<-den.t4.5
mod2fv<-lmer(Density~Treatment+Week+(1|Trial)+(1|Trial:deployment.day), data=df)
hist(resid(mod2fv))
qqnorm(resid(mod2fv))
qqline(resid(mod2fv))
anova(mod2fv,type="II")
mod2fv<-lmer(Density~Treatment*Week+(1|Trial)+(1|Trial:deployment.day), data=df)
hist(resid(mod2fv))
qqnorm(resid(mod2fv))
qqline(resid(mod2fv))
anova(mod2fv,type="I")
summary(mod2fv)
ranef(mod2fv)
mod2fvi<-lmer(Density~Treatment+Week+(1|Trial)+(1|Trial:deployment.day), data=df)
hist(resid(mod2fvi))
qqnorm(resid(mod2fvi))
qqline(resid(mod2fvi))
anova(mod2fvi,type="II")
summary(mod2fvi)
ranef(mod2fvi)
#reordering factors to get ranef
mod2fvi<-lmer(Density~Week+Treatment+(1|Trial)+(1|Trial:deployment.day), data=df)
hist(resid(mod2fvi))
qqnorm(resid(mod2fvi))
qqline(resid(mod2fvi))
anova(mod2fvi,type="II")
summary(mod2fvi)
ranef(mod2fvi)
anova(mod2fvi,type="II")
#no interaction, so dropped the interaction from the model and ran it as a type II anova
mod2fvi<-lmer(Density~Treatment+Week+(1|Trial)+(1|Trial:deployment.day), data=df)
anova(mod2fvi,type="II")
gob.den<-read.csv("Data/density.2019.3.6.csv)
gob.den<-read.csv("Data/density.2019.3.6.csv")
#setting up df for different years (will also include all of the figures for 2018 trials separately)####
den.t1.2.3<-gob.den[gob.den$Trial<4, ]
den.t4.5<-gob.den[(gob.den$Trial>3)&(gob.den$Trial<6), ]
den.t6<-gob.den[gob.den$Trial>5,]
#df assignments for analyses
df<-den.t1.2.3
library(sciplot)
library(lme4)
library(car)
library(ggplot2)
library(lmerTest)
library(dplyr)
#model building
mod1<-lm(den.max~Treatment, data=df)
hist(resid(mod1))
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(den.max~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
#2017 data only, running mixed model with nested term
mod2di<-lmer(den.max~Treatment*Day+(1|Trial)+(1|Trial:deployment.day), data=df)
hist(resid(mod2di))
qqnorm(resid(mod2di))
qqline(resid(mod2di))
anova(mod2di)
ranef(mod2di)
library(sciplot)
library(lme4)
library(car)
library(ggplot2)
library(lmerTest)
library(dplyr)
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
den.t4.5<-gob.den[(gob.den$Trial>3)&(gob.den$Trial<6), ]
df<-den.t4.5
#calculating means for abstracts
tapply(df$Density,df$Treatment,data=df)
#calculating means for abstracts
tapply(df$Density,df$Treatment,mean)
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt
library(sciplot)
library(lme4)
library(car)
library(ggplot2)
library(lmerTest)
library(dplyr)
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
#setting up df for different years (will also include all of the figures for 2018 trials separately)####
den.t1.2.3<-gob.den[gob.den$Trial<4, ]
den.t4.5<-gob.den[(gob.den$Trial>3)&(gob.den$Trial<6), ]
den.t6<-gob.den[gob.den$Trial>5,]
#df assignments for analyses
df<-den.t1.2.3
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt
df<-den.t4.5
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt
df<-den.t6
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt
#just looking at week 1 for trial 6
df<-den.t6[(den.t6$Week==1),]
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt
#model building
mod1<-lm(Density~Treatment, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
TukeyHSD(df$Density,df$Treatment)
TukeyHSD(df$Treatment,df$Density)
#just looking at week 1 for trial 6
df<-den.t6[(den.t6$Week==1),]
#model building
mod1<-lm(Density~Treatment, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
summary(mod1)
TukeyHSD(df$Treatment,df$Density)
plot(TukeyHSD(mod1,"Treatment"))
#model building
mod1<-aov(Density~Treatment, data=df)
plot(TukeyHSD(mod1,"Treatment"))
TukeyHSD((mod1,"Treatment",ordered=TRUE))
TukeyHSD((mod1,"Treatment", ordered=TRUE))
TukeyHSD(mod1,"Treatment", ordered=TRUE)
df<-den.t6
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
summary(mod2)
TukeyHSD(mod2,"Treatment",ordered=TRUE)
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-aov(Density~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
summary(mod2)
TukeyHSD(mod2,"Treatment",ordered=TRUE)
TukeyHSD(mod2,"Day",ordered=TRUE)
TukeyHSD(mod2,"Treatment",ordered=TRUE)
plot(TukeyHSD(mod2,"Treatment"))
library(tidyr)
library(sciplot)
library(lme4)
library(car)
library(ggplot2)
library(lmerTest)
library(dplyr)
library(tidyr)
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
gob.den <- gob.den %>%
mutate(Caging=ifelse(Treatment=="High"),"Caged","Uncaged"))
gob.den <- gob.den %>%
mutate(Caging=ifelse(Treatment=="High"),"Caged","Uncaged")
gob.den <- gob.den %>%
mutate(Caging=ifelse(Treatment="High"),"Uncaged","Caged")
gob.den <- gob.den %>%
mutate(Caging=ifelse((Treatment="High"),"Uncaged","Caged"))
gob.den <- gob.den %>%
mutate(Caging=ifelse((Treatment="High"),"Uncaged","Caged"))%>%
ifelse((Treatment="Control"),"Uncaged","Caged")
gob.den <- gob.den %>%
mutate(Caging=ifelse((Treatment="High"),"Uncaged","Caged"))%>%
ifelse((Treatment="Control"),"Uncaged","Caged"))
gob.den <- gob.den %>%
mutate(Caging=ifelse(Treatment="High","Uncaged",
ifelse(Treatment="Control","Uncaged","Caged")))
gob.den <- gob.den %>%
mutate(Caging=ifelse((Treatment="High"),"Uncaged",
ifelse(Treatment="Control"),"Uncaged","Caged"))
mutate(gob.den, Caging=ifelse((Treatment="High"),"Uncaged",
ifelse(Treatment="Control"),"Uncaged","Caged"))
mutate(gob.den, Caging=ifelse(Treatment="High","Uncaged",
ifelse(Treatment="Control","Uncaged","Caged")))
mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
View(gob.den)
gob.den <- gob.den %>%
mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
View(gob.den)
#gob.den <- gob.den %>%
gob.den.cage.uncaged<-mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
df<-gob.den.cage.uncaged
#model building
mod1<-aov(Density~Treatment, data=df)
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
gob.den<-mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
gob.den<-mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-mutate(gob.den, as.factor(Caging)=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
ifelse(Treatment=="Control","Uncaged","Caged"))
gob.den<-mutate(gob.den, as.factor(Caging)=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-mutate(gob.den, as.factor(Caging)=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged"))))
gob.den<-mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-mutate(gob.den, as.factor(Caging)=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den<-mutate(gob.den, Caging=ifelse(Treatment=="High","Uncaged",
ifelse(Treatment=="Control","Uncaged","Caged")))
gob.den$Caging<-as.factor(gob.den$Caging)
#setting up df for different years (will also include all of the figures for 2018 trials separately)####
den.t1.2.3<-gob.den[gob.den$Trial<4, ]
den.t4.5<-gob.den[(gob.den$Trial>3)&(gob.den$Trial<6), ]
den.t6<-gob.den[gob.den$Trial>5,]
df<-den.t6
#model building
mod1<-aov(Density~Caged, data=df)
#model building
mod1<-aov(Density~Caging, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
summary(mod1)
TukeyHSD(mod1,"Treatment", ordered=TRUE)
TukeyHSD(mod1,"Caging", ordered=TRUE)
plot(TukeyHSD(mod1,"Treatment"))
plot(TukeyHSD(mod1,"Caging"))
#model building
mod1<-aov(Density~Caging*Week, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
#just looking at week 1 for trial 6
df<-den.t6[(den.t6$Week==1),]
#model building
mod1<-aov(Density~Caging*Week, data=df)
#model building
mod1<-aov(Density~Caging, data=df)
hist(resid(mod1))
qqnorm(resid(mod1))
qqline(resid(mod1))
anova(mod1)
summary(mod1)
TukeyHSD(mod1,"Caging", ordered=TRUE)
plot(TukeyHSD(mod1,"Caging"))
#looking at caged vs. uncaged within week 1
bargraph.CI(x.factor = Day, response = Density,group = Caging, legend=TRUE, main="2018,trial 6, densities by treatment and day, week 1 data only", xlab="Day", ylab="Number of fish seen per reef (mean +/- se)",yleg=11, data = df)
#using den.max ("adjusted density counts") instead of raw den counts####
#df assignments for analyses
df<-den.t1.2.3
#using den.max ("adjusted density counts") instead of raw den counts####
#df assignments for analyses
df<-den.t1.2.3
#this is what I will need to do for trial 1-3, 2017, week isn't great becuase it was only 1
mod2<-lm(den.max~Treatment*Day, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
df<-den.t4.5
mod2<-lm(Density~Treatment*Week, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
df<-den.t6
mod2<-lm(Density~Treatment*Week, data=df)
hist(resid(mod2))
qqnorm(resid(mod2))
qqline(resid(mod2))
anova(mod2)
boxplot(Density~Treatment, data=df)
install.packages("pwr")
library(pwr)
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.35, sig.level = 0.05, power = )
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.0.02, sig.level = 0.05, power = )
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.002, sig.level = 0.05, power = )
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.02, sig.level = 0.05, power = )
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.15, sig.level = 0.05, power = )
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.10, sig.level = 0.05, power = )
#trial 6 power analysis for linear model
pwr.f2.test(u =3, v = 72, f2 = 0.05, sig.level = 0.05, power = )
gob.den<-read.csv("Data/density.2019.3.6.csv") #contains max.density counts now as well. 33% of all observations were
den.t6<-gob.den[gob.den$Trial>5,]
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
library(sciplot)
library(lme4)
library(car)
library(ggplot2)
library(lmerTest)
library(dplyr)
library(tidyr)
library(pwr)
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt<-df %>%
group_by(Treatment)
library(dplyr)
library(tidyr)
den.means.trt<-df %>%
group_by(Treatment)
df<-den.t6
den.means.trt<-df %>%
group_by(Treatment)
den.means.trt<-df %>%
group_by(Treatment) %>%
summarize(den.mean=mean(Density, na.rm=TRUE))
den.means.trt
den<-with(df, aggregate((den.max), list(Treatment=Treatment,Week=Week), mean))
den
library(sciplot)
library(lme4)
library(car)
library(lmerTest)
library(dplyr)
library(ggplot2)
library(MASS)
library(nlme)
library(pwr)
library(HH)#for ancova and plots
library(vegan)
library(agricolae)#for tukey post-hoc test
install.packages("vegan")
install.packages("agricolae")
#importing dataset, adding number of gobies on each reef, ordering treatments####
#includes a cloumn ("Treatment") where uncaged and HR are coded as "High"
#also includes a column ("T6.comparison") where uncaged and high are separated
viz.surv<-read.csv("Data/density.2019.10.1.csv", na.strings = "")
viz.surv$Density<-as.numeric(viz.surv$Density)
viz.surv$den.max<-as.numeric(viz.surv$den.max)
